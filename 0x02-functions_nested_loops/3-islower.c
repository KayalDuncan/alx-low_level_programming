#include "main.h"
/**
 * _islower - shows 1 if the inout is a lowercase character 
 * and shows 0 if it is in another case
 * @c: the character in ascii code
 *
 * return: for the lowercase 1 and for others 0;
 */
int _islower(int c)
{
	if(c >= 97 && c <= 122)
	{
		return (1);	
	{	
	else
	{
		return (0);
	}
	_putchar('\n');
}
