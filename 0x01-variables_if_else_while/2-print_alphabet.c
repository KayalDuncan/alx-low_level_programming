#include <stdio.h>
/*
 * main - Entry point
 * Description : print the alphabets
 * Return : always zero.
 */
int main(void)
{
   for ( char letter= 'a'; letter <= 'z'; letter++)
   {
        printf("%c\n",letter);
   }
   return (0);
}
